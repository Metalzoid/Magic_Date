<<<<<<< HEAD
<% @bookings.each do |booking| %>
<div class="card">  <%= booking.comment %> <%= link_to "avis", new_booking_rating_path(booking) %></div>

<% end %>
=======
<div class="cover mb-4 d-flex align-items-center justify-content-center">
  <div class="text-center">
    <h1>Mes commandes</h1>
    <h3 class="typed-js" data-controller="typed-js"></h3>
  </div>
</div>
<div class="container w-100">
  <div class="row pt-5 flex-wrap gap-2 justify-content-around">
      <% @bookings.each do |booking| %>
      <div class="card-booking col-5 p-0 border-0 mb-4">
          <div class="border-radius w-100 h-100">
                <div class="card-body p-5 rounded w-100 shadow bg-purple">
                    <p class="card-user">Réservé par : <%= booking.user.name %></p>
                    <p class="card-booking">Réservation : <%= booking.start_date %> au <%= booking.end_date %></p>
                    <p class="card-comment">Commentaire :
                      <% if booking.comment.size >= 80 %>
                        <%= booking.comment[0..80]%> ...
                      <% else %>
                        <%= booking.comment %>
                      <% end %></p>
                    <p class="card-statut">Statut de la commande : <%= booking.status %></p>
                    <p class="card-total-price">Prix total de la prestation : <%= booking.total_price %> €</p>
                </div>
          </div>
      </div>
      <% end %>
  </div>
</div>
>>>>>>> master
