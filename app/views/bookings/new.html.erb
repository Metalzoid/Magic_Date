  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8 form-container" data-controller="price-calcul" data-action="change->price-calcul#price" data-price-calcul-offerprice-value="<%= @offer.price %>">
        <div class="text-center mb-4" style="background: transparent;">
          <%= cl_image_tag @offer.user.photo.key, height: 150, gravity: "face", crop: "fill", style: "border-radius: 50%" %>
          <h4 class="my-2"><%= @offer.user.name %></h4>
        </div>
        <h2 class="text-center mb-4">Votre commande</h2>
        <div>
          <%= simple_form_for [@offer, @booking], data: { price_calcul_target: "form"} do |f| %>
            <%= f.input :start_date,
            as: :string,
            input_html: { data: { controller: "datepicker" } } %>
            <%= f.input :end_date,
            as: :string,
            input_html: { data: { controller: "datepicker" } } %>
            <%= f.input :comment , label: "Commentaire", as: :text %>
            <%= f.input :total_price, label: "Prix total:", input_html: { data: { price_calcul_target: "totalpricehidden" }, hidden: true }%>
            <div data-price-calcul-target="totalprice" class="px-1 mb-2" style="margin-top: 10px;">
              <h3></h3>
            </div>
            <%= f.button :submit, class: "btn btn-purple mb-5 mt-3", value: "Créer votre commande" %>
          <% end %>
        </div>

      </div>
    </div>
  </div>
