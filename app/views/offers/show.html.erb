
<div class="container text-center mt-5 bg-light rounded mb-4">
  <div class="row">
    <div class="col-8 offset-md-3">Offre de Audrey !</div>
  </div>
</div>

<div class="card mb-3" style="max-width: 700px;">
  <div class="row g-0">
    <div class="col-md-4">
     <%# <%= cl_image_tag(current_user.photo.key, width: 150, height: 150, crop: :thumb, gravity: :face, class: "img-fluid rounded-start" ) %> %>
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title"><h1><%= @offer.title %></h1></h5>
        <p class="card-text"><p><%= @offer.description %></p></p>
        <p class="card-text"><small class="text-body-secondary"><%= @offer.category %></small></p>
        <p><%= @offer.price %>
          <%= link_to 'Edit', edit_offer_path(@offer) %>
          <%= link_to 'Back', root_path %>
      </div>
    </div>
  </div>
</div>
